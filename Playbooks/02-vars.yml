- name: Demo on vars 
  hosts: all 
  vars:
    URL: vars.google.com 
  tasks:
    - name: Access Variable 
      debug:
        msg: "Helloo from {{URL}}" 

- name: Demo on vars from localhost
  hosts: localhost
  vars:
    URL: vars.google.com 
  tasks:
    - name: Access Variable 
      debug:
        msg: "Helloo from {{URL}}" 

- name: Demo on Vars Prompt
  hosts: all
  vars_prompt:
    - name: URL
      prompt: Enter URL
  tasks:
    - name: Access Variable 
      debug:
        msg: "Helloo from {{URL}}"      